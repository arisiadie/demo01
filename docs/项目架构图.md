# 项目架构图（逐字稿配套讲解版）

> 配套《项目讲解逐字稿.md》使用。下列图均为 Mermaid 格式，可在支持 Mermaid 的 Markdown 预览（VS Code、Typora、语雀、GitHub）或 PPT 插件中直接渲染展示。
>
> 系统定位：**基于 Agentic RAG 与多智能体协作的口腔医疗智能服务平台**（FastAPI 后端 + 原生前端 + Chroma 向量检索 + 五智能体调度 + DeepSeek LLM + MySQL 业务库）。
>
> 配色图例：🟦 前端 / 🟪 接入 / 🟧 编排 / 🟩 服务 / 🟨 检索 / ⬜ 数据。

---

## 图 1 · 系统总体分层架构

> 讲解用途：开场一分钟讲清"系统由哪几层组成，请求从上往下怎么流"。

```mermaid
flowchart TB
    subgraph CLIENT ["🟦 前端层 · app/static"]
        UI["单页前端 index.html / JS<br/>患者端 · 医生端 · 管理员端<br/>角色头部 X-User-Id / X-Role"]
    end

    subgraph API ["🟪 接入层 · app/api + app/main"]
        MAIN["FastAPI main.py<br/>启动初始化 · 静态挂载 · 提醒调度"]
        RL["限流中间件<br/>InMemoryRateLimitMiddleware"]
        ROUTES["routes.py 路由<br/>咨询 / 影像 / 档案 / 复核 / 管理"]
        DEPS["deps.py 鉴权依赖<br/>角色权限校验"]
    end

    subgraph CORE ["🟧 智能体编排层 · app/agents"]
        ORCH["OralAgentOrchestrator<br/>主调度器"]
        ROUTER["AgentRouter<br/>意图路由 / 计划"]
        GUARD["SafetyGuard<br/>医疗安全边界"]
        FLOW["AgenticRAGFlow<br/>LangChain Runnable 编排"]
        WF["MultiAgentWorkflow<br/>动态多智能体图 + 交接"]
    end

    subgraph SVC ["🟩 服务层 · app/services"]
        LLM["LLMClient<br/>DeepSeek + 本地兜底"]
        AUDIT["审计 / 告警 / 限流"]
        TRACE["可追溯 traceability"]
        PRIV["隐私合规 / 评测 / 通知"]
        SYNC["knowledge_sync 知识同步"]
    end

    subgraph RAG ["🟨 检索层 · app/rag"]
        STORE["KnowledgeStore<br/>关键词打分检索"]
        CHROMA["ChromaKnowledgeIndex<br/>向量检索 HashEmbedding"]
    end

    subgraph DATA ["⬜ 数据与模型层"]
        DB[("MySQL<br/>oralcare_agentic_rag<br/>33 张表")]
        KB[("示例知识库<br/>data/knowledge")]
        VEC[("Chroma 持久化<br/>data/chroma")]
        UP[("影像上传<br/>data/uploads")]
    end

    UI ==> RL
    RL ==> ROUTES
    MAIN -->|启动装配| ROUTES
    ROUTES --> DEPS
    ROUTES ==> ORCH
    ORCH ==> ROUTER
    ORCH ==> GUARD
    ORCH ==> FLOW
    ORCH ==> WF
    WF --> LLM
    ORCH --> LLM
    FLOW ==> STORE
    STORE ==> CHROMA
    ORCH --> SVC
    SVC --> DB
    STORE --> KB
    CHROMA --> VEC
    ROUTES --> UP
    SYNC --> STORE
    SYNC --> DB

    classDef client fill:#dbeafe,stroke:#2563eb,stroke-width:2px,color:#1e3a5f
    classDef api fill:#ede9fe,stroke:#7c3aed,stroke-width:2px,color:#3b0764
    classDef core fill:#ffedd5,stroke:#ea580c,stroke-width:2px,color:#7c2d12
    classDef svc fill:#dcfce7,stroke:#16a34a,stroke-width:2px,color:#14532d
    classDef rag fill:#fef9c3,stroke:#ca8a04,stroke-width:2px,color:#713f12
    classDef data fill:#f1f5f9,stroke:#475569,stroke-width:2px,color:#0f172a

    class UI client
    class MAIN,RL,ROUTES,DEPS api
    class ORCH,ROUTER,GUARD,FLOW,WF core
    class LLM,AUDIT,TRACE,PRIV,SYNC svc
    class STORE,CHROMA rag
    class DB,KB,VEC,UP data
```

---

## 图 2 · 一次咨询的端到端调用链

> 讲解用途：讲"用户问一句话，系统内部到底发生了什么"，是 demo 主线。

```mermaid
sequenceDiagram
    autonumber
    participant U as 👤 用户前端
    participant API as 🟪 FastAPI路由
    participant O as 🟧 Orchestrator
    participant R as AgentRouter
    participant S as SafetyGuard
    participant F as AgenticRAGFlow
    participant K as 🟨 检索层
    participant W as MultiAgentWorkflow
    participant L as 🟩 LLM_DeepSeek
    participant DB as ⬜ MySQL审计追溯

    U->>API: POST /api/consultations (message + 角色头)
    API->>O: run(AgentContext)

    rect rgb(255, 237, 213)
    note over O,R: 路由与安全校验
    O->>R: plan() 意图识别 + 检索计划
    R-->>O: 主智能体 / 次级智能体 / 风险信号
    O->>S: assess_message() 安全校验
    end

    alt 命中提示词注入 / 越权
        S-->>O: blocked
        O-->>API: 返回安全边界拒答
    else 通过
        rect rgb(254, 249, 195)
        note over F,K: Agentic RAG 多轮检索
        O->>F: 问题拆解 → 多轮检索 → 合并
        F->>K: retrieve(子问题, 分类, top_k)
        K-->>F: 命中来源 + 置信度
        end
        alt 无可引用来源
            F-->>O: 低置信度
            O-->>API: 低置信度拒答
        else 有证据
            rect rgb(220, 252, 231)
            note over O,W: 生成与多智能体协作
            O->>L: compose() 结构化生成
            L-->>O: 结论 / 依据 / 风险 / 下一步
            O->>W: run_workflow() 动态多智能体交接
            W->>L: 子智能体 LLM 调用
            W-->>O: 执行链 + 合并来源
            O->>O: 跨智能体交叉复核 + 医生复核标记
            end
            O->>DB: 写入审计 / 调用轨迹 / 来源绑定
            O-->>API: AgentResponse(含来源引用)
        end
    end
    API-->>U: 渲染结论 + 来源 + 风险 + 免责声明
```

---

## 图 3 · 五智能体调度与协作

> 讲解用途：讲核心卖点"多智能体协作 + 动态 workflow + 医生复核兜底"。

```mermaid
flowchart LR
    START(["🚀 开始<br/>用户请求"]) --> ROUTER{"🧭 意图路由<br/>AgentRouter"}

    ROUTER -->|症状相关| TRIAGE["🦷 症状预问诊<br/>智能体"]
    ROUTER -->|方案相关| TREAT["📋 诊疗方案解读<br/>智能体"]
    ROUTER -->|用药相关| MED["💊 用药合规审查<br/>智能体"]
    ROUTER -->|影像相关| IMG["🩻 影像报告解读<br/>智能体"]
    ROUTER -->|健康相关| HEALTH["🌱 健康管理科普<br/>智能体"]

    TRIAGE -->|需要方案| TREAT
    TRIAGE -->|涉及用药| MED
    TRIAGE -->|紧急| REVIEW
    TREAT -->|涉及用药| MED
    TREAT --> HEALTH
    TREAT -->|复杂方案| REVIEW
    MED -->|风险| REVIEW
    IMG --> TREAT
    IMG --> REVIEW

    REVIEW["🩺 医生复核入口"] --> END
    HEALTH --> END(["✅ 结束"])
    MED -->|安全| END

    REVIEW -.持久化.-> CFG[("⚙️ workflow 配置表<br/>管理员可动态编辑")]

    classDef startend fill:#e0f2fe,stroke:#0284c7,stroke-width:2px,color:#0c4a6e
    classDef router fill:#ede9fe,stroke:#7c3aed,stroke-width:2px,color:#3b0764
    classDef agent fill:#ffedd5,stroke:#ea580c,stroke-width:2px,color:#7c2d12
    classDef review fill:#fee2e2,stroke:#dc2626,stroke-width:2px,color:#7f1d1d
    classDef cfg fill:#f1f5f9,stroke:#475569,stroke-width:2px,color:#0f172a

    class START,END startend
    class ROUTER router
    class TRIAGE,TREAT,MED,IMG,HEALTH agent
    class REVIEW review
    class CFG cfg
```

---

## 图 4 · Agentic RAG 检索流程

> 讲解用途：讲"为什么叫 Agentic RAG，而不是一次检索"。

```mermaid
flowchart TB
    Q(["💬 用户问题 + 智能体类型"]) --> D["① 问题拆解 _decompose<br/>原问 + 维度子问 + 鉴别子问"]
    D --> M["② 多轮检索 _multi_retrieve<br/>逐子问检索 round_1..n"]
    M --> K1["KnowledgeStore<br/>关键词打分"]
    M --> K2["Chroma<br/>向量检索"]
    K1 --> MG["③ 结果合并去重 _merge<br/>按分数排序取 top_k"]
    K2 --> MG
    MG --> CONF{"④ 置信度评估<br/>轮次0.5 + 来源0.3 + 分类0.2"}
    CONF -->|无来源 / 低置信| REFUSE["⛔ 低置信度拒答<br/>建议线下就医"]
    CONF -->|达标| BIND["🔗 来源绑定 source_bindings<br/>结论 ↔ 证据可溯源"]
    BIND --> OUT(["✅ 交给智能体生成结构化结论"])

    classDef io fill:#e0f2fe,stroke:#0284c7,stroke-width:2px,color:#0c4a6e
    classDef step fill:#ffedd5,stroke:#ea580c,stroke-width:2px,color:#7c2d12
    classDef retrieve fill:#fef9c3,stroke:#ca8a04,stroke-width:2px,color:#713f12
    classDef decision fill:#ede9fe,stroke:#7c3aed,stroke-width:2px,color:#3b0764
    classDef refuse fill:#fee2e2,stroke:#dc2626,stroke-width:2px,color:#7f1d1d
    classDef ok fill:#dcfce7,stroke:#16a34a,stroke-width:2px,color:#14532d

    class Q,OUT io
    class D,M,MG step
    class K1,K2 retrieve
    class CONF decision
    class REFUSE refuse
    class BIND ok
```

**RAG 要点**
- 编排基于 `LangChain Runnable`：`拆解 → 多轮检索 → 合并` 三段链（[agentic_flow.py](../app/agents/agentic_flow.py)）。
- 每个智能体有专属子问题模板（牙位 / 用药禁忌 / 影像术语 / 疗程 / 护理阶段），实现"按领域多角度召回"。
- 检索同时走关键词打分与 Chroma 向量；**无来源即拒答**，保证不凭空作答，符合医疗安全边界。

---

## 调度要点（图 3 配套讲稿）

- 路由由 `AgentRouter.plan()` 给出主 / 次智能体与风险信号；workflow 按图边 + 智能体交接动作决定下一跳，并带**循环保护**（已执行的智能体不重复进入）。
- 任一智能体标记 `requires_review` 或触发风险 / 紧急，统一汇入**医生复核**节点。
- workflow 图结构可由管理员动态编辑并持久化到 MySQL（`WorkflowConfig / Node / Edge`），服务启动时从库恢复。

---

## 讲解串词建议（30 秒过渡）

1. **图 1** → "整个平台分五层，前端按角色发请求，经限流和鉴权进入智能体编排层，再调检索层和 LLM，最后落到 MySQL。"
2. **图 2** → "用户问一句牙痛，系统先路由意图、再做安全校验，然后 Agentic RAG 多轮检索，证据足够才让智能体生成，并写入完整审计轨迹。"
3. **图 3** → "五个智能体不是孤立的，它们按 workflow 图协作交接，任何风险都兜底到医生复核，且这张图管理员可以动态改。"
4. **图 4** → "我们的检索不是一次 query，而是拆成多个子问题多轮召回再合并，低置信度直接拒答——这是医疗场景的安全底线。"
